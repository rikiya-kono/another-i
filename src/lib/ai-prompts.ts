// System prompts for Another I AI

export const SYSTEM_PROMPT = `あなたは「Another I」- ユーザーのもう一人の自分です。
ユーザーの思考を整理し、構造化することが役割です。

対話の中で重要なポイントを抽出し、以下の形式で整理してください：
- 見出し（##）で大きなトピックを分類
- 箇条書きで具体的なポイントを列挙
- 重要な洞察は引用（>）で強調

ユーザーが「整理して」「まとめて」と言った場合は、これまでの会話を構造化してMarkdown形式で出力してください。

常に共感的で、ユーザーの考えを引き出すような質問をしてください。
日本語で応答してください。`;

export const SUMMARY_PROMPT = `
以下の会話内容を構造化されたMarkdownドキュメントにまとめてください。

形式:
## トピック名
- 重要なポイント1
- 重要なポイント2

> 重要な洞察や気づき

### 次のステップ
- アクションアイテム1
- アクションアイテム2
`;

export function generateMockResponse(userMessage: string): string {
    const responses = [
        "なるほど、それは興味深い視点ですね。もう少し詳しく教えていただけますか？",
        "その考えはとても大切だと思います。具体的にはどのような状況でそう感じますか？",
        "一緒に整理してみましょう。まず、一番気になっているポイントは何ですか？",
        "それぞれの選択肢について、メリット・デメリットを書き出してみましょうか？",
        "その気持ち、よく分かります。優先順位をつけるとしたら、何が一番重要ですか？",
    ];

    // Simple mock - in production, this would call the AI API
    return responses[Math.floor(Math.random() * responses.length)];
}

export function generateStructuredNote(messages: { role: string; content: string }[]): string {
    if (messages.length === 0) return '';

    const userMessages = messages.filter(m => m.role === 'user');
    const timestamp = new Date().toLocaleString('ja-JP');

    let note = `## 思考ログ - ${timestamp}\n\n`;

    userMessages.forEach((msg, index) => {
        note += `### ポイント ${index + 1}\n`;
        note += `${msg.content}\n\n`;
    });

    note += `---\n\n> 💡 この対話で ${userMessages.length} 件のトピックについて考えました。\n`;

    return note;
}
